{
  empty = {
    plugins.visual-multi.enable = true;
  };

  defaults = {
    plugins.visual-multi = {
      enable = true;
      settings = {
        default_mappings = false;
      };
    };
  };
}
